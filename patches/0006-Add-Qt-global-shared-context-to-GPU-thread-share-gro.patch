From 94f41e2fb91254c45d13868f7c54fa1305732a5e Mon Sep 17 00:00:00 2001
From: Peter Tworek <tworaz666@gmail.com>
Date: Sun, 22 Mar 2015 18:10:50 +0100
Subject: [PATCH 6/8] Add Qt global shared context to GPU thread share group.

This allows Qt to access GL resources produced by chromium GPU thread.

Signed-off-by: Peter Tworek <tworaz666@gmail.com>
---
 content/common/gpu/gpu_channel_manager.cc | 7 +++++++
 1 file changed, 7 insertions(+)

diff --git a/content/common/gpu/gpu_channel_manager.cc b/content/common/gpu/gpu_channel_manager.cc
index 42263ff..c77f802 100644
--- a/content/common/gpu/gpu_channel_manager.cc
+++ b/content/common/gpu/gpu_channel_manager.cc
@@ -26,6 +26,10 @@
 #include "ui/ozone/public/ozone_platform.h"
 #endif
 
+#if defined(USE_QT)
+#include "ui/gl/gl_share_context_qt.h"
+#endif
+
 namespace content {
 
 namespace {
@@ -191,6 +195,9 @@ void GpuChannelManager::OnEstablishChannel(int client_id,
   if (share_context) {
     if (!share_group_.get()) {
       share_group_ = new gfx::GLShareGroup;
+#if defined(USE_QT)
+      new gfx::GLShareContextQt(share_group_.get());
+#endif
       DCHECK(!mailbox_manager_.get());
       mailbox_manager_ = new gpu::gles2::MailboxManagerImpl;
     }
-- 
2.0.5

